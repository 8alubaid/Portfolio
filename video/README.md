# video/

Drop DemoSat test/flight footage here as **`demosat-test.mp4`**.

`project-1.html` references it directly. Until it exists, the media section gracefully falls back to a "footage coming soon" placeholder — nothing breaks if this file is missing.
