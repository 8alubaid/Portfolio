# img/

Drop your headshot here as **`my-photo.jpg`** (square, ≥800×800px recommended).

`index.html` references it directly. Until it exists, the hero avatar gracefully falls back to an "FB" monogram — nothing breaks if this file is missing.
